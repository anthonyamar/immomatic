<%= simple_form_for @real_estate, method: :put, url: real_estate_path(@real_estate), defaults: { label: false } do |f| %>
  <%= render 'real_estates/form', f: f, submit_message: "Modifier ce bien" %>
<% end %>